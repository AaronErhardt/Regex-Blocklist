{
  "description": "Filter HTTP requests with regex",
  "manifest_version": 2,
  "name": "RegEx Blacklist",
  "version": "1.0",

  "applications": {
    "gecko": {
      "id": "regex.blacklist@github.com",
      "strict_min_version": "60.0"
    }
  },

  "permissions": [
    "webRequest",
    "<all_urls>",
    "storage",
    "webRequestBlocking"
  ],

  "background": {
    "scripts": ["background.js"]
  },

  "options_ui": {
    "page": "options.html"
  }
}
